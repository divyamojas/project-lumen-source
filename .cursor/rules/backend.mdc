---
description: Lumen backend rules — apply to all Python files in this repo
globs: ["**/*.py", "requirements.txt", "Dockerfile"]
alwaysApply: true
---

# Lumen Backend

FastAPI service for the Lumen journaling PWA. Phase 1 scaffold complete.
Runs in Docker (lumen-api, port 8000). Connects to Supabase via Python client.

## Stack
- Python 3.11, FastAPI, Supabase Python client, PyJWT, Pydantic v2
- No ORM — Supabase client directly
- No TypeScript, no local installs — Docker only

## File Map
- `app/main.py` — app init, CORS, router mount, /health
- `app/auth.py` — `get_current_user` dependency: verifies JWT, returns user_id (sub claim)
- `app/routes/entries.py` — CRUD for /entries
- `app/models/entry.py` — EntryCreate, EntryUpdate, EntryResponse

## Auth
Every protected route uses `Depends(get_current_user)` from `app/auth.py`.

Resolution order:
1. Parse `Authorization: Bearer <token>` header
2. Peek at token `alg` header (unsigned)
3. Asymmetric (RS256/ES256): verify via JWKS at `SUPABASE_URL/auth/v1/.well-known/jwks.json`
4. HS256 + `SUPABASE_JWT_SECRET`: verify locally with shared secret
5. HS256 + no secret: verify remotely via `GET /auth/v1/user` with `SUPABASE_PUBLISHABLE_KEY`

Returns `user_id` (JWT `sub`) on success. Never trusts user_id from request body.

## Rules (non-negotiable)
- No ORM — Supabase Python client only
- No hardcoded secrets — all config from env vars (.env, never committed)
- user_id always from JWT, never from request body
- Return 404 (not 403) for owned-resource-not-found
- Return 401 for missing/invalid JWT
- CORS: allow http://localhost:3000

## Environment Variables (from .env)
- `SUPABASE_URL`
- `SUPABASE_SECRET_KEY` — preferred hosted backend key (`sb_secret_...`)
- `SUPABASE_PUBLISHABLE_KEY` — preferred public key (`sb_publishable_...`)
- `SUPABASE_JWKS_URL` — optional override
- `SUPABASE_SERVICE_ROLE_KEY` — optional legacy fallback
- `SUPABASE_ANON_KEY` — optional legacy fallback
- `SUPABASE_JWT_SECRET` — optional legacy HS256 fallback

## Out of Scope (Phase 1)
Do not implement: frontend wiring, S3, Lambda, Bedrock, Comprehend.
